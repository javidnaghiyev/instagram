import * as api from '../api'

export const signin = () => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}

export const signup = () => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}